// let result = ("Akif Ashfaq" && "akif123") ? "Login successful" : "Both fields are required";
// console.log(result)

let userName = '';
let passeword = ""

let result = (userName && passeword) ? "login sucessful" : "Invalid credentials"
console.log(result)